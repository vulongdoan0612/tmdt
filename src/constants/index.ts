export enum PAGE_TITLE {
    PREFIX = "FlashDeck | ",
    HOME="HOME",
  LOGIN = "LOGIN",
  PROFILE="PROFILE"
}
  
export enum MESSAGE_TOAST {
    LOG_OUT_SUCCESS = 'Đăng xuất thành công!',
    LOG_OUT_FAILED = 'Đăng xuất không thành công!',
}
export enum SESSION_STORAGE_KEY {
  ACC_TAB = 'accTab',
  ACC_TAB_OLD = 'acc_tab_old',
  BINDING = 'binding',
}